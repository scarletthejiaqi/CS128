#include <iostream>

#include "dna_strand.hpp"


int main() {
    // DNAstrand dna;
    // Node* a = new Node('a');
    // Node* b = new Node('a',a);
    // Node* c = new Node('a',b);
    // Node* d = new Node('a',c);
    // Node* e = new Node('a',d);
    // // Node* f = new Node('f',e);
    // dna.puthead(e);
    // dna.puttail(a);
    // const char* pattern = "aaa";
    // DNAstrand seq;
    // Node* a1 = new Node('z');
    // Node* b1 = new Node('y',a1);
    // Node* c1 = new Node('x',b1);
    // Node* d1 = new Node('w',c1);

    // seq.puthead(d1);
    // seq.puttail(a1);
    // dna.SpliceIn(pattern,seq);
    // Node* each = dna.gethead();
    // while(each != nullptr) {
    //     std::cout<<each->data<<std::endl;
    //     each = each->next;
    // }

}

