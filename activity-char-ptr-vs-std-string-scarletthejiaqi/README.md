# Activity: `const char *` vs `char []` vs  `std::string`
For 3/31 Lesson Activity

## Compilation / Execution:
Similar to the MPs, use `make exec` to compile the code and `./bin/exec` to execute the program.
